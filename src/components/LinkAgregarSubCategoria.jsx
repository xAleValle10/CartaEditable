import { Link } from "react-router-dom";

export const LinkAgregarSubCategoria = () => {
  return (
    <Link to="/addSubCategory">
      <span className="cursorhover material-symbols-outlined">library_add</span>
    </Link>
  );
};
